# --- Python cache / build ---
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# --- Virtual envs ---
venv/
.api_venv/
.env/
api/venv/

# --- Env files / secrets ---
.env
api/.env

# --- SQLite DBs & memory ---
api/memory/
memory/
archive/db/
archive/api/tamor_backup_before_phase7.db

# --- Uploads & embeddings ---
api/uploads/
api/embeddings/

# --- Logs ---
logs/
*.log
archive/logs/

# --- Node / frontend build ---
ui/node_modules/
ui/dist/
ui/.vite/
archive/ui_old_2025-11-29/node_modules/
archive/ui/node_modules/

# --- Zips / archives ---
*.zip
tamor
# --- Additional SQLite DBs ---
api/*.db
api/*.db.*
*.db
*.db.*

# Wrong/backup DB variants
api/**/WRONG_*
api/**/empty_*
api/**/EMPTY_*

# --- Backup / copy files from manual edits ---
* (copy).py
* (copy).jsx
* (copy).js
* (copy).css
* (copy).html

# --- Python cache (extra safety) ---
*.pyc
*.pyo

# runtime caches
/data/playlists/tmdb_cache.json

# runtime caches
/data/playlists/tmdb_cache.json

# runtime data
/data/playlists/*.json
!data/playlists/.keep

data/playlists/*.json
!data/playlists/*.example.json\n
